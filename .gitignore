node_modules/
src/vendor/
.claude/
.agents/
skills-lock.json
read-aloud-chrome-store.zip

# extension/ build output IS committed on purpose (not gitignored) so people can
# download the repo and load it straight into Chrome with no build step required.
